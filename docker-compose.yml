version: '3'

services:

  redis:
    image: sickp/alpine-redis:3.2.2
    restart: unless-stopped

  server:
    build: .
    command: python server.py
    volumes:
      - ./:/app
    links:
      - redis
    ports:
      - "8080:8080"
